# Production web build template used by tools/publish-web.ps1.
# SAFE TO COMMIT only while values below stay empty / public.
# NEVER put live API keys, tokens, or service-role credentials here.
# Local secrets belong in .env (gitignored). OPENAI_API_KEY must remain blank.
KNOWLEDGE_CDN_URL=https://destroyer.jonbailey.xyz/knowledge
OPENAI_API_KEY=
